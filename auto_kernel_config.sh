#!/bin/bash

# ??????????
export KCONFIG_AUTOCONFIG=1
export KCONFIG_AUTOSAVE=1

# ??????
echo "???? make olddefconfig..."
make olddefconfig || {
  echo "make olddefconfig????? make defconfig..."
  make defconfig || {
    echo "make defconfig????????????..."
    make clean
    make defconfig
  }
}
