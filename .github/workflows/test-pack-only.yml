# .github/workflows/test-pack-only.yml
name: 🔧 Test Pack Only (Hardcoded RootFS)

on:
  workflow_dispatch:  # 手动触发

jobs:
  download-and-pack:
    runs-on: ubuntu-22.04
    steps:
      - name: Download rootfs from hardcoded URL
        run: |
          echo "📥 Downloading rootfs..."
          wget -O rootfs.tar.gz "https://github.com/SaeTheOne/openwrt3/releases/download/1.0/immortalwrt-24.10.4-armsr-armv8-generic-rootfs.tar.gz"
          ls -lh rootfs.tar.gz

      - name: Upload as artifact
        uses: actions/upload-artifact@v4
        with:
          name: openwrt-rootfs-for-packing
          path: ./rootfs.tar.gz

  pack:
    needs: download-and-pack
    uses: ./.github/workflows/pack-image.yml
    with: {}  # 👈 关键！必须显式写 with（即使为空）
    secrets: inherit
