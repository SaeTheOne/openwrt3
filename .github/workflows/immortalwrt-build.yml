- name: Free disk space
  run: |
    # 清理不必要的软件包
    sudo apt-get autoremove -y
    sudo apt-get clean
    
    # 清理Docker镜像（如果不需要）
    sudo docker system prune -af
    
    # 扩展根分区（适用于GitHub Actions的Ubuntu环境）
    sudo rm -rf /usr/share/dotnet
    sudo rm -rf /opt/ghc
    sudo rm -rf /usr/local/share/boost
    sudo rm -rf "$AGENT_TOOLSDIRECTORY"
