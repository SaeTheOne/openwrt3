name: OpenWRT Builder

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/openwrt-builder.yml'
      - 'configs/**'
      - 'patches/**'
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      config:
        description: 'Config file name (without .config) - 如需使用自定义配置，上传到configs目录并指定文件名'
        required: true
        default: 'default'

jobs:
  build:
    runs-on: ubuntu-22.04
    env:
      OPENWRT_SRC_URL: https://github.com/openwrt/openwrt.git
      OPENWRT_BRANCH: openwrt-23.05
      FEEDS_CONF: feeds.conf.default
      CONFIG_FILE: ${{ github.event.inputs.config || 'default' }}
      OUTPUT_DIR: bin/targets/*/*

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Initialization environment
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          # 🔧 修复：安装完整依赖（含 Python3 相关，解决 python3-distutils 依赖警告）
          sudo apt update -y
          sudo apt install -y build-essential clang flex bison g++ gawk gcc-multilib \
            gettext git libncurses5-dev libssl-dev python3 python3-pip python3-setuptools \
            rsync subversion unzip wget zlib1g-dev file jq curl

      - name: Clone OpenWRT source
        run: |
          git clone --depth 1 -b $OPENWRT_BRANCH $OPENWRT_SRC_URL openwrt
          cd openwrt
          # 🔧 优化：使用源码自带配置，更新并安装 feeds（自动处理部分依赖修复）
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Apply patches
        run: |
          cd openwrt
          if [ -d ../patches ]; then
            for patch in $(find ../patches -name "*.patch" | sort); do
              echo "Applying patch: $patch"
              patch -p1 < $patch || exit 1
            done
          fi

      - name: Load config
        run: |
          cd openwrt
          # 优先检查是否有本地配置文件
          if [ -f ../configs/$CONFIG_FILE.config ]; then
            echo "✅ 找到 configs/$CONFIG_FILE.config 文件，优先使用"
            cp ../configs/$CONFIG_FILE.config .config
          else
            echo "❌ 未找到本地配置文件，将从OpenWrt镜像站下载配置文件"
            # 根据分支信息构造下载链接（ARM架构）
            if [[ "$OPENWRT_BRANCH" == openwrt-* ]]; then
                # 提取版本号（去掉openwrt-前缀）
                VERSION=${OPENWRT_BRANCH#openwrt-}
                
                # 处理版本号格式
                if [[ "$VERSION" == v* ]]; then
                    # v开头的版本号（如v23.05.0）
                    CLEAN_VERSION=${VERSION#v}
                    CONFIG_URL="https://downloads.openwrt.org/releases/$CLEAN_VERSION/targets/armsr/armv8/config.buildinfo"
                elif [[ "$VERSION" =~ ^[0-9]+\.[0-9]+$ ]]; then
                    # 主版本号（如23.05），添加.0子版本号
                    CONFIG_URL="https://downloads.openwrt.org/releases/$VERSION.0/targets/armsr/armv8/config.buildinfo"
                else
                    # 完整版本号（如23.05.0）
                    CONFIG_URL="https://downloads.openwrt.org/releases/$VERSION/targets/armsr/armv8/config.buildinfo"
                fi
                
                echo "🔗 正在从版本发布地址下载ARM架构配置: $CONFIG_URL"
              else
                # 快照版本
                CONFIG_URL="https://downloads.openwrt.org/snapshots/targets/armsr/armv8/config.buildinfo"
                echo "🔗 正在从快照地址下载ARM架构配置: $CONFIG_URL"
            fi
            
            # 下载config.buildinfo
            wget -O .config "$CONFIG_URL" || {
              echo "❌ 下载配置文件失败！"
              exit 1
            }
          fi
          # 🔧 修复：自动安装缺失的依赖包（关键！解决依赖警告）
          make defconfig
          # 🔧 优化：删除冗余的 python3-distutils 依赖声明（批量处理，添加容错处理）
          if [ -d package/feeds ]; then
            find package/feeds -name "Makefile" -type f -print0 | xargs -0 -r sed -i '/DEPENDS.*python3-distutils/d' || echo "📝 没有找到需要处理的Makefile文件或处理已完成"
          else
            echo "📝 package/feeds目录不存在，跳过依赖清理步骤"
          fi

      - name: Download packages
        run: |
          cd openwrt
          make download -j$(nproc)
          # 🔧 优化：缓存下载的包，加速下次编译
          find dl -type f -exec sha256sum {} \; > ../dl-cache.sha256

      - name: Build firmware
        run: |
          cd openwrt
          # 🔧 优化：使用多线程编译（根据 CPU 核心数调整）
          make -j$(nproc) V=s
          # 🔧 修复：检查编译结果，确保固件生成
          if [ ! -d "$OUTPUT_DIR" ]; then
            echo "Firmware build failed! No output directory found."
            exit 1
          fi

      - name: Organize output files
        run: |
          cd openwrt
          mkdir -p ../output
          # 🔧 优化：复制固件和校验文件，重命名便于识别
          cp -r $OUTPUT_DIR/* ../output/
          cd ../output
          for file in $(find . -name "*.bin" -o -name "*.img"); do
            mv $file $(basename $file .bin)-$CONFIG_FILE.bin
          done
          # 生成校验和文件
          sha256sum * > firmware-sha256.txt

      - name: Compile firmware
        id: compile
        run: |
          cd openwrt
          echo -e "$(nproc) thread compile"
          make -j$(nproc) || make -j1 || make -j1 V=s
          echo "status=success" >> $GITHUB_OUTPUT
          # 使用更安全的方式设置环境变量，避免格式错误
          # 为DEVICE_NAME提供默认值，确保不会出现空值问题
          echo "DEVICE_NAME=_armvirt_64" >> "$GITHUB_ENV"
          
          if grep -q '^CONFIG_TARGET.*DEVICE.*=y' .config; then
              DEV_NAME=$(grep '^CONFIG_TARGET.*DEVICE.*=y' .config | sed -r 's/.*DEVICE_(.*)=y/\1/' | tr -d '\r\n')
              if [ -n "$DEV_NAME" ]; then
                  echo "DEVICE_NAME=_${DEV_NAME}" >> "$GITHUB_ENV"
              fi
          fi
          echo "FILE_DATE=_$(date +"%Y%m%d%H%M")" >> "$GITHUB_ENV"

      - name: Check space usage
        if: ${{ !cancelled() }}
        run: df -hT

      - name: Upload bin directory
        uses: actions/upload-artifact@v4
        if: ${{ steps.compile.outputs.status == 'success' && env.UPLOAD_BIN_DIR == 'true' }}
        with:
          name: OpenWrt_bin${{ env.DEVICE_NAME }}${{ env.FILE_DATE }}
          path: openwrt/bin

      - name: Organize firmware files
        id: organize
        if: ${{ env.UPLOAD_FIRMWARE == 'true' && !cancelled() }}
        run: |
          cd openwrt/bin/targets/*/*
          ROOTFS=$(find "$PWD" -maxdepth 1 -name "*-generic-rootfs.tar.gz" | head -n1)
          if [ -z "$ROOTFS" ]; then
            ROOTFS=$(find "$PWD" -maxdepth 1 -name "*-rootfs.tar.gz" | head -n1)
          fi
          if [ -z "$ROOTFS" ]; then
            echo "❌ No rootfs tarball found!"; ls -la; exit 1
          fi
          ls -lh "$ROOTFS"
          BASENAME=$(basename "$ROOTFS")
          # 使用标准格式设置输出变量，避免file命令错误
          echo "rootfs_path=${ROOTFS}" >> "${GITHUB_OUTPUT}"
          echo "rootfs_basename=${BASENAME}" >> "${GITHUB_OUTPUT}"
          echo "firmware_path=${PWD}" >> "${GITHUB_OUTPUT}"
          echo "✅ Found rootfs: $ROOTFS"

      - name: Upload rootfs artifact for packing
        uses: actions/upload-artifact@v4
        if: ${{ steps.organize.outputs.rootfs_path && !cancelled() }}
        with:
          name: ${{ steps.organize.outputs.rootfs_basename }}
          path: ${{ steps.organize.outputs.rootfs_path }}

      - name: Clean up after build
        if: always()
        run: |
          echo "🧹 清理构建环境..."
          sudo rm -rf /workdir/*
          docker system prune -af 2>/dev/null || true
          echo "✅ 构建环境清理完成，虚拟机将自动关闭"

  pack:
    needs: build
    uses: ./.github/workflows/pack-image.yml
    with:
      rootfs_path: ${{ needs.build.outputs.rootfs_path }}
      kernel_base_url: https://raw.githubusercontent.com/SaeTheOne/openwrt3/main/flippy_kernel
